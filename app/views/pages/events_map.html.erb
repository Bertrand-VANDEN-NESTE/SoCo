<div class="container">
  <div class="d-flex justify-content-center">
    <div class="btn btn-cards mt-3 mb-3"><%= link_to "Evènements", events_path, class: "text-light"%></div>
  </div>
  <div class="card-trip-map margin">
    <div style="width: 100%; height: 80vh;"
      data-controller="mapindex"
      data-mapindex-markers-value="<%= @markers.to_json %>"
      data-mapindex-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
    <div>
    <% @events.first(5).each do |event| %>
      <div class="margin">
        <%= link_to event_path(event) do %>
          <div class="card-trip radius h-25 m-3">
            <div class="card-trip-infos">
              <div class="d-flex justify-content-evently">
                <%= cl_image_tag event.user.photo.key, class: "h-25 w-25 rounded-circle object-fit-cover" %>
                <h2 class="text-center m-auto p-0"><%= event.title %></h2>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
  </div>
</div>
